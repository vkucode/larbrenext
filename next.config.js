/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["larbreapains.fr", "res.cloudinary.com"], // Adăugăm res.cloudinary.com aici
  },
};

module.exports = nextConfig;
